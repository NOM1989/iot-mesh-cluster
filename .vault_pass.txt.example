# Copy to .vault_pass.txt and replace with a real password (one line, no trailing newline).
# Generate one with:  openssl rand -base64 32 > .vault_pass.txt && chmod 600 .vault_pass.txt
# The real file is gitignored.
